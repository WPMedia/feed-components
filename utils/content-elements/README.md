# Feeds Content Elements

A library to process the various content_element types.

## functions

buildContent(element, numRows, resizerKey, resizerURL, width, height)
absoluteUrl(url, domain)
buildContentCorrection(element)
buildContentEndorsement(element)
buildContentGallery(element, resizertKey. resizerURL, width, height)
buildContentImage(element, resizerKey, resizerURL, width, height)
buildContentLinkList(element)
buildContentList(element)
buildContentListElement(element)
buildContentNumericRating(element)
buildContentOembed(element)
buildContentQuote(element)
buildContentText(element)
buildContentTable(element)
buildContentText(element)
buildContentVideo(element)
buildContentInterstitial(element, domain)
